{
  "version": 2,
  "builds": [
    {
      "src": "api-backup/**/*.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api-backup/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/api-backup/$1"
    }
  ],
  "functions": {
    "api-backup/**/*.ts": {
      "maxDuration": 30
    }
  },
  "env": {
    "NODE_ENV": "production"
  }
}
